set(WORKFLOW_ROOT_BUILD_DIR "${GLOBAL_OUTPUT_DIR}/Altera-Ody-Engine")

if(CMAKE_BUILD_TYPE STREQUAL "Debug")
    set(WF_PREFIX "${WORKFLOW_ROOT_BUILD_DIR}/Debug")
else()
    set(WF_PREFIX "${WORKFLOW_ROOT_BUILD_DIR}/Release")
endif()

# include_directories(CUDA)
# 
# set(CUDA_FILES
#     "${CMAKE_CURRENT_SOURCE_DIR}/CUDA/main.cu"
# )

# ------------------------
# Add subdirectories
# ------------------------

add_subdirectory(App)
add_subdirectory(Engine) 